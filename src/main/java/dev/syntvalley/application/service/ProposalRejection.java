package dev.syntvalley.application.service;

/** Why a build project proposal was refused before any project record was created. */
public enum ProposalRejection {
    UNKNOWN_TEMPLATE,
    NO_SITE
}
