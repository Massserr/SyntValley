package dev.syntvalley.domain.citizen;

/** Slice 3 Citizen lifecycle. Entity presence is tracked separately and is not a lifecycle state. */
public enum CitizenLifecycle {
    ACTIVE,
    DECEASED
}
